<div class="form-group">
    <button type="submit" class="btn btn-primary">
        <i class="fas fa-filter"></i> {{ __('actions.filter') }}
    </button>
    <a href="javascript:void(0)" onclick="resetFilters()" class="btn btn-secondary">
        <i class="fas fa-redo"></i> {{ __('actions.reset') }}
    </a>
</div>
